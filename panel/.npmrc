# Tailwind 4 compila con un binario nativo que viene como dependencia opcional,
# una por plataforma. En Railway npm las omitia y el build moria con "Cannot
# find native binding". Esto lo obliga a instalarlas.
include=optional
